# https://taskfile.dev

version: '3'

tasks:
  test:
    dir: '{{.USER_WORKING_DIR}}'
    cmds:
      - echo "tests are not implemented yet!"
  
  run:
    dir: '{{.USER_WORKING_DIR}}'
    cmds: 
      - ./out.exe

  build:
    dir: '{{.USER_WORKING_DIR}}'
    sources: ["**/*.cpp"]
    cmds:
      - g++ -Wall -o out.exe main.cpp

